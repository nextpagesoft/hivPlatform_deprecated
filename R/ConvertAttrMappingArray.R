ConvertAttrMappingArray <- function(
  x,
  session,
  inputname
) {
  # attrMapping <- rbindlist(lapply(
  #   x,
  #   function(row) {
  #     jsonlite:::null_to_na(row)
  #   }
  # ))

  return(x)
}
